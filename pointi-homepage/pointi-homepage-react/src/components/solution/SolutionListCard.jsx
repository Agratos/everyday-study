import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div``;

const SolutionListCard = ({isClick ,list}) => {
    //console.log(list);
    return (
        <Wrapper>

        </Wrapper>
    )
}

export default SolutionListCard;